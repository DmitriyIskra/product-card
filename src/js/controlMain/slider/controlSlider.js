export default class ControlSlider {
    constructor(drawUI) {
        this.drawUI = drawUI;
    }

    init() {
        // console.log(this.darawUI);
        this.drawUI.rendering();
        
    }
}